---
// Section heading — mono eyebrow + display title + optional link.
import Icon from '~/components/ui/Icon.astro';

interface Props {
  eyebrow?: string;
  title: string;
  href?: string;
  linkLabel?: string;
  class?: string;
}
const { eyebrow, title, href, linkLabel = 'View all', class: className = '' } = Astro.props;
---

<div class={`flex items-end justify-between gap-6 ${className}`}>
  <div>
    {eyebrow && <p class="eyebrow mb-2 text-accent-strong">{eyebrow}</p>}
    <h2 class="masthead text-3xl uppercase sm:text-4xl">{title}</h2>
  </div>
  {
    href && (
      <a
        href={href}
        class="group hidden shrink-0 items-center gap-1.5 pb-1 text-sm font-medium text-graphite transition-fluid hover:text-ink sm:inline-flex"
      >
        {linkLabel}
        <Icon name="arrowRight" size={16} class="transition-transform group-hover:translate-x-0.5" />
      </a>
    )
  }
</div>
